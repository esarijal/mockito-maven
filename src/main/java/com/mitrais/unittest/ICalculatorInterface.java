package com.mitrais.unittest;

public interface ICalculatorInterface {
    int add(int first, int second);
    int reduce(int number, int reducer);
    int multiply(int first, int second);
    double divide(int dividend, int divider);
}
